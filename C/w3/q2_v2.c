// Print out a list of prime numbers from 1 to N
/*I think this is a better way to check if a number is prime or not*/

#include <stdio.h>

int main(int argc, char *argv[]) {
  int a = 0, n, i, count = 0;
  scanf("%d", &n);
